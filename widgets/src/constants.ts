import type { Pair } from './types/pair';

export const sizeRange: Pair<number> = [0.4, 1.4];
export const hueRange: Pair<number>  = [0.3, 0.8];
export const zRange: Pair<number> = [-3, 3];

export const numModels = 9;
export const numEpochs = 100;

export const gridSize = 10;
